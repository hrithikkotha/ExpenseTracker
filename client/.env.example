# ── Client environment (Phase 0) ────────────────────────────────
# Copy to `.env`. All Vite client vars must be prefixed with VITE_.

# Base URL for the API. In dev, the Vite proxy also forwards /api to
# http://localhost:4000, so the relative default works out of the box.
VITE_API_URL=/api/v1
